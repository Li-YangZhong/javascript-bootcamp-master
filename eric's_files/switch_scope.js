let x = 1

switch (x) {
    case 1: {
        let message = 'x is one'
        console.log(message)
        break;
    }

    case 2: {

    }

    default: {
        let message = 'x is' + x
        console.log(message)
    }

}